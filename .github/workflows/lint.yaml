on:
  - pull_request

name: Lint

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Install Task
        run: |
          sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run Unit Tests
        run: task lint
